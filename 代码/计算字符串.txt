#include <stdio.h>
#include<string.h>
int count(char s[]) {
	int len = strlen(s);
	int alp, geo, bla, sym;
	for (int i = 0; i < len; i++) {
		if (s[i] >= 'A' && s[i] <= 'Z' && s[i] >= 'a' && s[i] <= 'z') alp++;
		if (s[i] == 0) bla++;
		if()
	}
}