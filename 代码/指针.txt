1.相对路径，不建议绝对路径
2.指针使函数返回多个值
3.野指针：无定向指向
4.地址为0的地方一般不能访问
5.修改调用者里的数据（指针的优点？）用简单的指针处理复杂的数据结构
6.传递指针指向，而不是指针本身。
7.指针算数运算，移动的基本单位是其所指的元素，而不是字节
8.a[i]=*[a+i]
9.指针存放地址大小
10.变量消失/不存在：变量所占内存空间可有其他用途
11.指针互换执行速度快